# Nicolas Carlo's dotfiles

This is a collection of dotfiles and configuration files I use to customize my tools.

You need to create symlinks manually, like this:

```
ln -s <path-to-dotfiles>/gitconfig ~/.gitconfig
```
